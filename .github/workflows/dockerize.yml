name: Dockerize

on:
  push:
    branches:
      - main

jobs:
  dockerize:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # auto-generated
      DOCKER_BUILDKIT: 1

    steps:
      # checkout repository code
      - uses: actions/checkout@v4

      # docker QEMU preparation
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # docker Buildx preparation
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # docker login
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.dockerhub_username }}
          password: ${{ secrets.dockerhub_password }}

      # docker build & push amd64
      - name: Build and push for x64_86 processor architecture
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{secrets.dockerhub_username}}/${{secrets.dockerhub_repo_name}}:intel
          platforms: linux/amd64
          cache-from: type=registry,ref=${{secrets.dockerhub_username}}/${{secrets.dockerhub_repo_name}}:cache
          cache-to: type=inline,mode=max
          build-args: |
            CABO_CLIENT_BUILD_COMMIT_ID=${{ github.sha }}
            CABO_CLIENT_BUILD_COMMIT_TIMESTAMP=${{ github.event.head_commit.timestamp }}
            CABO_SERVER_BUILD_COMMIT_ID=${{ vars.CABO_SERVER_BUILD_COMMIT_ID }}
            CABO_SERVER_BUILD_COMMIT_TIMESTAMP=${{ vars.CABO_SERVER_BUILD_COMMIT_TIMESTAMP }}
